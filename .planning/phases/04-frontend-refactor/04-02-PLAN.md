---
phase: 04-frontend-refactor
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - products.js
  - index.html
autonomous: true

must_haves:
  truths:
    - "Engraving product cards are rendered entirely from Supabase data -- no static engraving cards exist in index.html"
    - "3D prints product cards are rendered entirely from Supabase data -- no static prints3d cards exist in index.html"
    - "Color-dependent products (snake, star fidget, spinner, etc.) route to View Details for customization instead of inline color pickers"
    - "The existingSlugs deduplication logic is removed from both dynamic renderers"
  artifacts:
    - path: "products.js"
      provides: "Rewritten renderDynamicEngravingProducts and renderDynamicPrints3dProducts that render ALL products from Supabase"
      contains: "grid.innerHTML"
    - path: "index.html"
      provides: "Empty product grids with no static product-card divs for engraving or prints3d"
      contains: "id=\"prints3dGrid\""
  key_links:
    - from: "products.js"
      to: "Supabase products table"
      via: "products array (loaded by loadProducts)"
      pattern: "products\\.filter.*category"
    - from: "products.js renderDynamicPrints3dProducts"
      to: "navigateToProduct"
      via: "onclick handler on dynamic cards"
      pattern: "navigateToProduct"
---

<objective>
Eliminate the dual product system: remove all 61 static product cards from index.html and rewrite the dynamic renderers to render ALL engraving and 3D prints products from Supabase.

Purpose: FE-02 requirement. Single source of truth for product data -- Supabase is the only product source.
Output: Clean index.html with empty grids, dynamic renderers that populate everything from DB.
</objective>

<execution_context>
@C:/Users/sammy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sammy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-frontend-refactor/04-RESEARCH.md
@.planning/phases/04-frontend-refactor/04-01-SUMMARY.md
@products.js
@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove all 61 static product cards from index.html</name>
  <files>index.html</files>
  <action>
Remove ALL static `<div class="product-card ...">` blocks for both categories:

1. **54 prints3d-product cards** (approximately lines 538-1337 in current index.html): Remove every `<div class="product-card prints3d-product ...">...</div>` block. Keep the grid container: `<div class="products-grid" id="prints3dGrid">` -- it must remain but be EMPTY (no child elements).

2. **7 engraving-product cards** (approximately lines 1386-1640+ in current index.html): Remove every `<div class="product-card engraving-product ...">...</div>` block. Keep the grid container: `<div class="products-grid" id="engravingGrid">` -- it must remain but be EMPTY.

Do NOT remove:
- The grid container divs themselves (prints3dGrid, engravingGrid)
- Any section headers or category navigation above the grids
- The filter/sort controls if they exist
- Any non-product-card HTML between or around the grids
- Product cards for OTHER categories (arches, columns, walls, centerpieces, services) if they exist as static HTML

After removal, verify the line count of index.html decreased significantly (~800+ lines removed from the 61 cards).
  </action>
  <verify>
1. `grep -c 'class="product-card prints3d-product' index.html` returns 0
2. `grep -c 'class="product-card engraving-product' index.html` returns 0
3. `grep -c 'id="prints3dGrid"' index.html` returns 1 (container preserved)
4. `grep -c 'id="engravingGrid"' index.html` returns 1 (container preserved)
  </verify>
  <done>All 61 static product cards removed from index.html; grid containers preserved empty</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite dynamic renderers to render ALL products from Supabase</name>
  <files>products.js</files>
  <action>
Rewrite `renderDynamicEngravingProducts` and `renderDynamicPrints3dProducts` in products.js:

**renderDynamicEngravingProducts:**
1. Remove the `existingSlugs` logic entirely (no more reading slugs from existing DOM cards)
2. Clear the grid completely: `grid.innerHTML = '';`
3. Filter ALL engraving products: `const engravingProducts = products.filter(p => p.category === 'engraving');`
4. For each product, generate a card with:
   - `class="product-card engraving-product"` and `data-material` attribute
   - Clickable product image div (onclick -> navigateToProduct)
   - Product image with fallback to emoji icon
   - Sale/Popular badges if applicable
   - Product name (set via textContent, NOT innerHTML -- per SEC decision from Phase 1)
   - Product description (set via textContent)
   - Size label if product has size
   - Price display (use existing price formatting logic)
   - For tiered-pricing products: render pricing tiers if the product has them
   - "View Details" button (onclick -> navigateToProduct) -- NOT direct add-to-cart, since color-dependent products need the detail page
5. Use `card.querySelector('.product-name').textContent = product.name;` pattern for user-derived fields (XSS prevention per Phase 1 decision)

**renderDynamicPrints3dProducts:**
1. Same changes as engraving: remove existingSlugs, clear grid, render ALL prints3d products
2. Filter: `const prints3dProducts = products.filter(p => p.category === 'prints3d');`
3. Same card structure but:
   - Use `object-fit: contain` class for 3D print images (not cover)
   - All products get "View Details" button (no inline color selectors)
   - This resolves the color-picker specialty product problem: Snake, Star Fidget, Spinner, Flexi Fish, etc. all route to the product detail page where color selection and customization UI already exists in renderProductDetail

**Color-picker specialty products resolution:**
The static cards for Snake, Star Fidget, Spinner, Flexi Fish, Octagon Fidget, Infinity Cube, Flexi Dino, Snail, Twisty Lizard contained inline `<select>` color pickers. After this change, ALL products show a "View Details" button that navigates to the product detail page (renderProductDetail), where customization options are handled. The specialty `addSnakeToCart()` etc. functions in cart.js remain intact -- they are called from the product detail page, not the grid cards.

**Important:** Preserve the `renderDynamicProducts()` wrapper function that calls both renderers -- other code calls this function.
  </action>
  <verify>
1. Open site in browser, navigate to 3D Prints page -- all products render from Supabase data
2. Navigate to Engraving page -- all products render from Supabase data
3. Click any product card -- navigates to product detail page
4. On product detail page for a color-dependent product (e.g., Snake), color selection UI appears
5. `grep -c 'existingSlugs' products.js` returns 0
6. No console errors
  </verify>
  <done>Both dynamic renderers clear and rebuild grids entirely from Supabase data; no static cards remain; all products route through View Details for customization; existingSlugs deduplication removed</done>
</task>

</tasks>

<verification>
1. Browse to 3D Prints category -- product count matches Supabase `SELECT count(*) FROM products WHERE category = 'prints3d'`
2. Browse to Engraving category -- product count matches Supabase query
3. Click a color-dependent product (Snake, Star Fidget) -- detail page shows color picker
4. Add product to cart from detail page -- works correctly
5. `grep -c 'product-card.*prints3d' index.html` returns 0
6. `grep -c 'product-card.*engraving' index.html` returns 0
7. No `existingSlugs` references in products.js
</verification>

<success_criteria>
- Zero static product cards for engraving or prints3d in index.html
- Dynamic renderers produce ALL category products from Supabase
- Color-dependent products work via product detail page
- existingSlugs deduplication logic eliminated
</success_criteria>

<output>
After completion, create `.planning/phases/04-frontend-refactor/04-02-SUMMARY.md`
</output>
