---
phase: 04-frontend-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - products.js
  - cart.js
  - checkout.js
  - staff.js
  - ui.js
  - index.html
autonomous: true

must_haves:
  truths:
    - "Browser network tab shows products.js, cart.js, checkout.js, staff.js, and ui.js loaded as ES modules"
    - "app.js is no longer loaded by index.html"
    - "All existing site functionality works identically after the split (cart, checkout, navigation, staff portal, gallery)"
  artifacts:
    - path: "products.js"
      provides: "Product data loading, rendering, filtering, navigation, catalog"
      contains: "export"
    - path: "cart.js"
      provides: "Cart state, add/remove functions, cart UI"
      contains: "export const cart"
    - path: "checkout.js"
      provides: "Checkout flow, payment, coupons, orders, modals (Waiver/Contract/NoRefund/BookingGate/ColorPalette)"
      contains: "export"
    - path: "staff.js"
      provides: "Staff portal login, product CRUD, orders, barcode scanner"
      contains: "window.handleStaffLogin"
    - path: "ui.js"
      provides: "Navigation, hero slideshow, gallery, initializeApp bootstrap"
      contains: "export function initializeApp"
    - path: "index.html"
      provides: "Module script tags replacing app.js"
      contains: "type=\"module\""
  key_links:
    - from: "products.js"
      to: "cart.js"
      via: "import { cart, saveCart }"
      pattern: "import.*from.*cart"
    - from: "ui.js"
      to: "products.js"
      via: "import { loadProducts, products, renderCatalog, renderServices }"
      pattern: "import.*from.*products"
    - from: "ui.js"
      to: "cart.js"
      via: "import { updateCartCount, renderCartItems }"
      pattern: "import.*from.*cart"
    - from: "checkout.js"
      to: "cart.js"
      via: "import { cart, saveCart, getGrandTotal }"
      pattern: "import.*from.*cart"
    - from: "checkout.js"
      to: "products.js"
      via: "import { products }"
      pattern: "import.*from.*products"
---

<objective>
Split app.js (5,633 lines) into 5 ES modules: products.js, cart.js, checkout.js, staff.js, ui.js

Purpose: FE-01 requirement. The monolith must be decomposed before 04-02 and 04-03 can modify the correct files.
Output: 5 new JS module files, updated index.html script tags, app.js no longer referenced.
</objective>

<execution_context>
@C:/Users/sammy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sammy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-frontend-refactor/04-RESEARCH.md
@app.js
@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract app.js into 5 ES module files</name>
  <files>products.js, cart.js, checkout.js, staff.js, ui.js</files>
  <action>
Split app.js into 5 module files following the function-to-module map in 04-RESEARCH.md. All code in app.js is indented with 8 spaces (no wrapper function) -- dedent all code to 0 base indentation when extracting.

**products.js** (lines 1-111, 113-231, 257-281, 2117-2165, 2172-2401, 2402-2560):
- Contains: DB_PRODUCTS_URL, DB_ANON_KEY, DB_FETCH_HEADERS, loadProducts, loadProductImages, renderDynamicProducts, renderDynamicEngravingProducts, renderDynamicPrints3dProducts, gradients, categoryLabels, currentFilter, createProductCard, navigateToProduct, bookConsultation, getProductBySlug, getCategoryPage, renderProductDetail, changeProductImage, getProductFeatures, renderCatalog, renderServices, filterProducts, filterEngravingProducts, filter3DProducts, inquireProduct, formatPhone, selectContactService, handleSubmit
- CRITICAL SHARED STATE: Declare `const products = [];` (not `let`). Change the reassignment at current line 23 (`products = dbProducts.map(...)`) to in-place mutation: `products.length = 0; products.push(...dbProducts.map(p => ({...})));`. Also change the error handler at line 61 (`products = [];`) to `products.length = 0;`. This ensures all modules importing `products` see the same array reference.
- Export: `export { products, loadProducts, renderCatalog, renderServices, renderDynamicProducts, createProductCard, navigateToProduct, getProductBySlug, renderProductDetail, filterProducts, filterEngravingProducts, filter3DProducts }`
- Import from cart.js: `import { cart, addToCart, saveCart, updateCartCount } from './cart.js';` (for functions that reference cart)
- Import from checkout.js: `import { applyCoupon, removeCoupon } from './checkout.js';` ONLY if products.js actually calls those -- check first; if not needed, omit.
- The `loadProducts` function calls `initializeApp()` when `skipInit` is false. Use dynamic import to break the circular dependency: `const { initializeApp } = await import('./ui.js');` inside the loadProducts function body (not at top level).
- Remove the DOMContentLoaded/readyState guards for loadProducts -- that boot logic moves to ui.js.
- Attach all onclick-referenced functions to `window`: navigateToProduct, addToCart (if defined here -- but addToCart is in cart.js, so NOT here), filterProducts, filterEngravingProducts, filter3DProducts, bookConsultation, changeProductImage, inquireProduct, selectContactService, handleSubmit, formatPhone.

**cart.js** (lines 282-416, 417-1219, 1354-1395):
- Contains: cart array init, SHIPPING_RATE, FREE_SHIPPING_THRESHOLD, TAX_RATE, saveCart, cartHasProducts, cartHasServicesOnly, getProductSubtotal, getServiceSubtotal, getProductDiscount, getDiscountedProductSubtotal, getShippingCost, getTaxAmount, getGrandTotal, updateShippingVisibility, updateCartCount, addToCart, ALL add*ToCart specialty functions (addEngravingToCart, addEngravingToCartFromDetail, addTieredEngravingToCart, addTieredKeychainToCart, addCustomKeychainToCart, addCustomizableKeychainToCart, addTieredAcrylicToCart, addTieredWoodRoundsToCart, addTieredCoasterToCart, updateStarFidgetPrice, addStarFidgetToCart, addSpinnerToCart, addFlexiFishToCart, addSnakeToCart, addHeartGiftBoxToCart, addMoonAndBackToCart, addHeartCoasterSetToCart, addRoundStarCoasterSetToCart, addSquareCoffeeBeanCoasterToCart, addHexagonShapedCoasterToCart, addOctagonFidgetToCart, addInfinityCubeToCart, addFlexiDinoToCart, addSnailToCart, addTwistyLizardToCart), removeFromCart, clearCart, getCartTotal, toggleCart, renderCartItems, goToCheckout
- Export: `export { cart, saveCart, cartHasProducts, cartHasServicesOnly, getProductSubtotal, getServiceSubtotal, getProductDiscount, getDiscountedProductSubtotal, getShippingCost, getTaxAmount, getGrandTotal, updateCartCount, addToCart, removeFromCart, clearCart, getCartTotal, toggleCart, renderCartItems, goToCheckout, updateShippingVisibility }`
- Export `const cart = JSON.parse(localStorage.getItem('partyPalaceCart')) || [];` -- this is already an array that gets mutated in place via push/splice, so no reassignment issue.
- Import from products.js: `import { products } from './products.js';` (addToCart does product lookups)
- Window exports: ALL add*ToCart functions, removeFromCart, clearCart, toggleCart, goToCheckout, updateCartCount, addToCart, addEngravingToCart, addEngravingToCartFromDetail, addTieredEngravingToCart, addTieredKeychainToCart, addCustomKeychainToCart, addCustomizableKeychainToCart, addTieredAcrylicToCart, addTieredWoodRoundsToCart, addTieredCoasterToCart, updateStarFidgetPrice, addStarFidgetToCart, addSpinnerToCart, addFlexiFishToCart, addSnakeToCart, addHeartGiftBoxToCart, addMoonAndBackToCart, addHeartCoasterSetToCart, addRoundStarCoasterSetToCart, addSquareCoffeeBeanCoasterToCart, addHexagonShapedCoasterToCart, addOctagonFidgetToCart, addInfinityCubeToCart, addFlexiDinoToCart, addSnailToCart, addTwistyLizardToCart

**checkout.js** (lines 1237-1353, 1408-1925, 2621-3589):
- Contains: applyCoupon, removeCoupon, showCouponMessage, getDiscountedTotal, updateCheckoutWithDiscount, renderCheckoutItems, initPaymentOptions, updatePaymentUI, updatePaymentButtonText, getSelectedPaymentAmount, getPaymentType, createOrderInSupabase, displaySuccessOrderSummary, saveSignedDocument, showNotification, handleCheckoutSubmit, submitViaEmail, updateProductOptions, handleCustomOrderSubmit, WaiverModal, ContractModal, NoRefundModal, BookingGate, ColorPalette objects
- Import from cart.js: `import { cart, saveCart, cartHasProducts, cartHasServicesOnly, getProductSubtotal, getServiceSubtotal, getProductDiscount, getDiscountedProductSubtotal, getShippingCost, getTaxAmount, getGrandTotal, updateCartCount, clearCart, updateShippingVisibility } from './cart.js';`
- Import from products.js: `import { products } from './products.js';` (if renderCheckoutItems references products)
- Window exports: applyCoupon, removeCoupon, handleCheckoutSubmit, handleCustomOrderSubmit, updateProductOptions, updatePanelPrice (if it exists)
- Export: `export { WaiverModal, ContractModal, NoRefundModal, BookingGate, ColorPalette, applyCoupon, removeCoupon, initPaymentOptions, renderCheckoutItems, showNotification }`

**staff.js** (lines 3873-5633):
- Contains: ALL staff* functions, barcode scanner functions, staffProducts, staffUser, staffPortalInitialized, staffCurrentFilter, etc.
- This module accesses `window.supabaseClient` -- use `window.supabaseClient` explicitly (not a bare `supabaseClient` reference) since supabase-client.js is a plain script.
- Import: NONE from other modules (staff.js is self-contained per research). If initStaffPortal references functions from other modules, import them; but per research staff.js is isolated.
- Export: `export { initStaffPortal }`
- Window exports: handleStaffLogin, handleStaffLogout, handleStaffProductSubmit, handleStaffDeleteProduct, openStaffProductModal, editStaffProduct, confirmStaffDelete, openStaffModal, closeStaffModal, toggleDiscountField, filterOrders, viewOrderDetail, saveOrderStatus, updateOrderStatus, exportOrdersCSV, exportProductsCSV, exportCustomersCSV, searchCustomers, renderCustomerList, buildCustomerList, filterActivityLog, toggleProductSelection, toggleSelectAll, sortStaffTable, clearSelection, bulkDeleteSelected, switchStaffImageTab, handleStaffFileSelect, removeStaffImage, openStaffScannerModal, closeStaffScannerModal, startBarcodeCamera, fillFormFromScan, lookupBarcode

**ui.js** (lines 234-255, 1926-2116, 3590-3838, 3839-3871):
- Contains: initHeroSlideshow, navigate, initPageFromHash, toggleMobileMenu, closeMobileMenu, popstate/pageshow event listeners, renderGallery, initGalleryFilters, filterGallery, getCategoryForProduct, openLightbox, closeLightbox, changeLightboxImage, updateLightboxImage, createLightbox, openProductLightbox, initializeApp
- Import from products.js: `import { loadProducts, products, renderCatalog, renderServices, renderDynamicProducts, createProductCard, navigateToProduct, renderProductDetail, filterProducts } from './products.js';`
- Import from cart.js: `import { cart, updateCartCount, renderCartItems, toggleCart } from './cart.js';`
- Import from checkout.js: `import { WaiverModal, ContractModal, NoRefundModal, BookingGate, ColorPalette, initPaymentOptions, renderCheckoutItems } from './checkout.js';`
- Import from staff.js: `import { initStaffPortal } from './staff.js';`
- Export: `export { initializeApp, navigate, initHeroSlideshow }`
- Window exports: navigate, toggleMobileMenu, closeMobileMenu, openLightbox, closeLightbox, changeLightboxImage, filterGallery
- Boot logic at bottom of ui.js: `loadProducts(false);` -- this triggers the chain (loadProducts -> renderDynamicProducts -> initializeApp via dynamic import)
- Keep the DOMContentLoaded guard for initHeroSlideshow at top level of ui.js (the slideshow init runs independently of product loading)

**Cross-module dependency audit before writing files:**
1. Read through app.js carefully to identify every function call that crosses module boundaries
2. For each call, ensure the target function is either imported or accessed via window
3. Pay special attention to: navigate() called from products.js (import from ui.js or use window.navigate), showNotification() called from various places, updateCartCount() called after cart modifications

**Avoid these pitfalls:**
- Do NOT reassign the `products` array -- always mutate in place (products.length = 0; products.push(...))
- Do NOT forget window.X exports for ANY function referenced in index.html onclick attributes
- Do NOT create circular imports at the top level -- use dynamic import() for the products.js -> ui.js (initializeApp) call
- Do NOT change any business logic -- this is a pure mechanical extraction
- Do NOT remove the DOMContentLoaded/readyState check for initHeroSlideshow
- Make sure `product_image_urls.js` script tag stays BEFORE all module script tags in index.html
  </action>
  <verify>
Open index.html in browser. Verify:
1. Network tab shows products.js, cart.js, checkout.js, staff.js, ui.js loaded (no app.js)
2. Products load and render on the home page
3. Cart add/remove works
4. Navigation between pages works
5. Staff portal login works
6. Gallery renders
7. No console errors related to undefined functions
8. Browser console: `typeof window.addToCart === 'function'` returns true
9. Browser console: `typeof window.navigate === 'function'` returns true
  </verify>
  <done>All 5 module files exist, app.js is no longer loaded, and the site functions identically to before the split with zero console errors from missing functions</done>
</task>

<task type="auto">
  <name>Task 2: Update index.html script tags and remove app.js reference</name>
  <files>index.html</files>
  <action>
In index.html, replace the script tags at lines 3339-3340:

**Current (line 3339-3340):**
```html
<script src="product_image_urls.js?v=4"></script>
<script src="app.js?v=4"></script>
```

**Replace with:**
```html
<script src="product_image_urls.js"></script>
<script type="module" src="ui.js"></script>
```

Key details:
- `product_image_urls.js` stays as a plain (non-module) script and MUST come first -- it sets `window.productGalleryImages` which products.js reads
- Remove the `?v=4` cache busters (modules are fetched with different caching semantics)
- Only `ui.js` needs a script tag in HTML -- it is the root entry point that imports all other modules (products.js, cart.js, checkout.js, staff.js). The browser fetches those automatically via the import graph.
- The supabase CDN and supabase-client.js script tags (lines 122-123) stay unchanged -- they must load before staff.js needs `window.supabaseClient`
- The Stripe script tag (line 126) stays unchanged
- The inline script blocks at lines 3343+ and 3502+ stay unchanged -- they are self-contained or use window-exported functions
- Do NOT add separate script tags for cart.js, checkout.js, products.js, staff.js -- they are imported by ui.js

**Audit inline scripts at lines 3343-3500 and 3502-3741:**
- Line 3343 block: check if it calls `navigate()` or other functions from modules. If so, those functions must be on `window` (handled in Task 1).
- Line 3502 block: cursor effect -- self-contained, no changes needed.

Do NOT delete app.js from disk yet -- it serves as reference. It simply will not be loaded.
  </action>
  <verify>
1. View page source: only one module script tag (`ui.js`), product_image_urls.js as plain script before it
2. Network tab: ui.js loads, then products.js, cart.js, checkout.js, staff.js load via import graph
3. No 404 errors for any script
4. All inline script blocks still function (love question section, cursor effect)
  </verify>
  <done>index.html loads ui.js as the single ES module entry point; app.js script tag is removed; all other script tags preserved; browser loads all 5 modules via import chain</done>
</task>

</tasks>

<verification>
1. Open the site in browser -- products render, cart works, checkout flow works, staff portal works, gallery works
2. Network tab shows: ui.js, products.js, cart.js, checkout.js, staff.js all loaded as ES modules (Content-Type or initiator column shows "module")
3. No reference to app.js in network requests
4. Console has zero "X is not defined" or "X is not a function" errors
5. Test cart: add item, verify count updates, open cart sidebar, remove item
6. Test navigation: click Products, Engraving, 3D Prints, Services, Gallery, Contact -- all pages render
7. Test staff portal: navigate to staff page, login form appears
</verification>

<success_criteria>
- app.js monolith is no longer loaded by the browser
- 5 ES module files exist and are loaded via import graph from ui.js
- All ~70 onclick-referenced functions are accessible on window
- products array uses in-place mutation (not reassignment)
- Zero functional regressions
</success_criteria>

<output>
After completion, create `.planning/phases/04-frontend-refactor/04-01-SUMMARY.md`
</output>
