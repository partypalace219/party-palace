---
phase: 03-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app.js
autonomous: true

must_haves:
  truths:
    - "The Popular badge on product cards is driven by the Supabase featured column"
    - "The hardcoded popularProducts array no longer exists in app.js"
    - "All dynamically rendered product img tags include loading=lazy"
  artifacts:
    - path: "app.js"
      provides: "Product rendering with DB-driven featured flag and lazy-loaded images"
      contains: "loading=\"lazy\""
  key_links:
    - from: "app.js (product.popular)"
      to: "Supabase featured column"
      via: "p.featured mapping at line 32"
      pattern: "popular: p\\.featured"
---

<objective>
Remove the hardcoded popularProducts override and add lazy loading to all dynamic product images.

Purpose: CLN-02 requires Popular badges to be driven by the Supabase `featured` column (not a hardcoded array). CLN-03 requires all dynamic `<img>` tags to include `loading="lazy"`. Both changes are in app.js and are small, independent edits.
Output: app.js updated with 2 changes.
</objective>

<execution_context>
@C:/Users/sammy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sammy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cleanup/03-RESEARCH.md
@app.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove hardcoded popularProducts override and verify Supabase featured values</name>
  <files>app.js</files>
  <action>
BEFORE editing app.js, verify the 7 products have `featured = true` in Supabase. Run a query via the supabase CLI or check the database directly. The products are: Specialty Arch, Chiara Arch, Spiral Columns, Flower Walls, Balloon Centerpieces, Vases, Custom Lithophane.

If any of those 7 do NOT have `featured = true`, update them in Supabase first:
```sql
UPDATE products SET featured = true WHERE name IN ('Specialty Arch', 'Chiara Arch', 'Spiral Columns', 'Flower Walls', 'Balloon Centerpieces', 'Vases', 'Custom Lithophane');
```

Then delete lines 50-54 of app.js (the comment and the 4 lines of popularProducts code):
```javascript
                // Set popular products (override database values)
                const popularProducts = ['Specialty Arch', 'Chiara Arch', 'Spiral Columns', 'Flower Walls', 'Balloon Centerpieces', 'Vases', 'Custom Lithophane'];
                products.forEach(product => {
                    product.popular = popularProducts.includes(product.name);
                });
```

After removal, `product.popular` is already set from `p.featured` at line 32: `popular: p.featured`. No replacement code needed.

To query Supabase, use the project URL from MEMORY.md: `https://nsedpvrqhxcikhlieize.supabase.co`. Use curl with the anon key from the app's existing JS, or check the .env / server config for the key.
  </action>
  <verify>
1. `grep -n "popularProducts" app.js` returns no matches
2. `grep -n "popular: p.featured" app.js` returns a match (confirming the DB-driven path exists)
  </verify>
  <done>The hardcoded popularProducts array is removed. The Popular badge is now driven entirely by the Supabase `featured` column value mapped at product load time.</done>
</task>

<task type="auto">
  <name>Task 2: Add loading="lazy" to all dynamic product img tags</name>
  <files>app.js</files>
  <action>
Add `loading="lazy"` attribute to img tags at these locations in app.js (line numbers are APPROXIMATE after Task 1's deletion shifts lines up by 5):

1. **renderDynamicEngravingProducts** (~line 163): The img tag inside the template literal. Add `loading="lazy"` after the `alt=""` attribute:
   Before: `<img src="" alt="" style="width: 100%; ...`
   After:  `<img src="" alt="" loading="lazy" style="width: 100%; ...`

2. **renderDynamicPrints3dProducts** (~line 215): Same pattern as above.
   Before: `<img src="" alt="" style="width: 100%; ...`
   After:  `<img src="" alt="" loading="lazy" style="width: 100%; ...`

3. **createProductCard** (~line 2126): Two img tags on the same line — primary and secondary images.
   Add `loading="lazy"` to BOTH img tags:
   - Primary: `<img src="${product.images[0]}" alt="${product.name}" loading="lazy" class="primary-image" ...`
   - Secondary: `<img src="${product.images[1]}" alt="${product.name} alternate view" loading="lazy" class="secondary-image">`

4. **renderProductDetail main image** (~line 2253): The main product detail image.
   Before: `<img src="${product.images[0]}" alt="" id="productMainImage" onclick="openProductLightbox(0)" style="cursor: pointer;">`
   After:  `<img src="${product.images[0]}" alt="" id="productMainImage" loading="lazy" onclick="openProductLightbox(0)" style="cursor: pointer;">`

5. **renderProductDetail thumbnails** (~line 2269): The thumbnail img inside the .map() call.
   Before: `<img src="${img}" alt="${product.name} ${idx + 1}">`
   After:  `<img src="${img}" alt="${product.name} ${idx + 1}" loading="lazy">`

DO NOT modify these existing img tags:
- Line ~3626 (gallery image — already has `loading="lazy"`)
- Line ~3742 (lightbox — empty src placeholder)
- Line ~4872 (staff thumbnail — admin-only, not customer-facing)
  </action>
  <verify>
1. Count lazy loading attributes: `grep -c 'loading="lazy"' app.js` should return at least 7 (5 new + at least 1 existing gallery + possibly others)
2. Verify specific locations: `grep -n 'loading="lazy"' app.js` and confirm matches appear near the renderDynamicEngravingProducts, renderDynamicPrints3dProducts, createProductCard, and renderProductDetail functions
3. Verify excluded lines: the lightbox img (empty src) and staff thumbnail should NOT have loading="lazy"
  </verify>
  <done>All 6 dynamic customer-facing product img tags include `loading="lazy"`. The gallery image that already had it is unchanged. Lightbox and staff images are untouched.</done>
</task>

</tasks>

<verification>
1. `grep "popularProducts" app.js` returns no output
2. `grep -c 'loading="lazy"' app.js` returns 7+ (6 product images + 1 existing gallery)
3. The site loads without JS errors — open the browser console and confirm no exceptions from product rendering
</verification>

<success_criteria>
The Popular badge is driven by the Supabase `featured` column with no hardcoded override. All dynamic product img tags include `loading="lazy"`. No regressions in product rendering.
</success_criteria>

<output>
After completion, create `.planning/phases/03-cleanup/03-02-SUMMARY.md`
</output>
