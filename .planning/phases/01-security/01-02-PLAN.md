---
phase: 01-security
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase-functions/create-checkout-session/index.ts
  - supabase-functions/stripe-webhook/index.ts
  - supabase-functions/send-contact-email/index.ts
  - supabase-functions/save-signed-document/index.ts
autonomous: true

must_haves:
  truths:
    - "A request from an unlisted origin to any of the 4 edge functions receives Access-Control-Allow-Origin set to the production domain (not the request's origin), causing the browser to reject the response"
    - "A request from https://thepartypalace.in receives Access-Control-Allow-Origin: https://thepartypalace.in on all responses including errors"
    - "OPTIONS preflight, 200 success, 400 error, and 500 error responses all include CORS headers"
    - "Localhost development origins (localhost:3000, localhost:5500, 127.0.0.1:5500) are permitted"
  artifacts:
    - path: "supabase-functions/create-checkout-session/index.ts"
      provides: "Dynamic CORS via getCorsHeaders(req)"
      contains: "ALLOWED_ORIGINS"
    - path: "supabase-functions/stripe-webhook/index.ts"
      provides: "CORS headers on all responses"
      contains: "ALLOWED_ORIGINS"
    - path: "supabase-functions/send-contact-email/index.ts"
      provides: "Dynamic CORS replacing wildcard"
      contains: "getCorsHeaders"
    - path: "supabase-functions/save-signed-document/index.ts"
      provides: "Dynamic CORS replacing wildcard"
      contains: "getCorsHeaders"
  key_links:
    - from: "getCorsHeaders(req)"
      to: "every new Response() call"
      via: "corsHeaders variable spread into headers"
      pattern: "corsHeaders"
    - from: "ALLOWED_ORIGINS array"
      to: "Access-Control-Allow-Origin header"
      via: "origin inclusion check"
      pattern: "ALLOWED_ORIGINS\\.includes"
---

<objective>
Replace wildcard CORS (`Access-Control-Allow-Origin: *`) with dynamic origin validation on all 4 edge functions.

Purpose: SEC-03 requires CORS locked to thepartypalace.in. Currently all 4 edge functions allow any origin, meaning any website can make authenticated API calls to these endpoints.

Output: All 4 edge functions use a shared `getCorsHeaders(req)` pattern that checks the request origin against an allowlist and returns appropriate headers. Every response (success, error, preflight) includes CORS headers.
</objective>

<execution_context>
@C:/Users/sammy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sammy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/sammy/OneDrive/Documents/Projects/party-palace/.planning/PROJECT.md
@C:/Users/sammy/OneDrive/Documents/Projects/party-palace/.planning/phases/01-security/01-RESEARCH.md
@C:/Users/sammy/OneDrive/Documents/Projects/party-palace/supabase-functions/create-checkout-session/index.ts
@C:/Users/sammy/OneDrive/Documents/Projects/party-palace/supabase-functions/stripe-webhook/index.ts
@C:/Users/sammy/OneDrive/Documents/Projects/party-palace/supabase-functions/send-contact-email/index.ts
@C:/Users/sammy/OneDrive/Documents/Projects/party-palace/supabase-functions/save-signed-document/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace wildcard CORS with dynamic origin validation in all 4 edge functions</name>
  <files>
    supabase-functions/create-checkout-session/index.ts
    supabase-functions/send-contact-email/index.ts
    supabase-functions/save-signed-document/index.ts
  </files>
  <action>
In each of the 3 files listed above (create-checkout-session, send-contact-email, save-signed-document), apply the same CORS pattern:

1. **Replace the static `corsHeaders` constant** at the top of the file:

   REMOVE:
   ```typescript
   const corsHeaders = {
     'Access-Control-Allow-Origin': '*',
     'Access-Control-Allow-Headers': 'authorization, x-client-info, apikey, content-type',
   }
   ```

   ADD:
   ```typescript
   const ALLOWED_ORIGINS = [
     'https://thepartypalace.in',
     'http://localhost:3000',
     'http://localhost:5500',
     'http://127.0.0.1:5500',
   ]

   function getCorsHeaders(req: Request) {
     const origin = req.headers.get('Origin') || ''
     const allowedOrigin = ALLOWED_ORIGINS.includes(origin) ? origin : ALLOWED_ORIGINS[0]
     return {
       'Access-Control-Allow-Origin': allowedOrigin,
       'Access-Control-Allow-Headers': 'authorization, x-client-info, apikey, content-type',
     }
   }
   ```

2. **Update the serve() handler** — at the very start of the handler, before any logic, compute CORS headers:
   ```typescript
   const corsHeaders = getCorsHeaders(req)
   ```
   This makes `corsHeaders` a per-request local variable. All existing code that references `corsHeaders` continues to work without changes.

3. **Verify every `new Response(...)` in the file includes `corsHeaders`** in its headers. Audit each response:
   - OPTIONS preflight response
   - Success responses (200)
   - Error responses (400, 429, 500)
   - Catch block responses
   
   If any response is missing corsHeaders, add them. This is especially important for error responses.

**For create-checkout-session:** The existing code already spreads `corsHeaders` into all responses. Just replace the constant with the function + per-request variable.

**For send-contact-email:** Same pattern — already uses `corsHeaders` in all responses. Replace constant, add `const corsHeaders = getCorsHeaders(req)` at start of handler.

**For save-signed-document:** Same pattern. Replace constant, add per-request variable.
  </action>
  <verify>
For each of the 3 files:
- Search for `'Access-Control-Allow-Origin': '*'` — should return zero matches
- Search for `ALLOWED_ORIGINS` — should find the array
- Search for `getCorsHeaders` — should find the function definition and usage
- Count `new Response(` calls and verify each has `corsHeaders` in headers
  </verify>
  <done>
All 3 edge functions (create-checkout-session, send-contact-email, save-signed-document) use dynamic origin validation. Wildcard CORS is removed. Every response includes CORS headers.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add CORS headers to stripe-webhook edge function</name>
  <files>supabase-functions/stripe-webhook/index.ts</files>
  <action>
The stripe-webhook function is unique — it currently has NO CORS headers at all (it receives calls from Stripe, not from a browser). However, SEC-03 requires CORS lockdown on ALL edge functions for consistency and defense-in-depth.

1. **Add the ALLOWED_ORIGINS array and getCorsHeaders function** at the top of the file (same code as other functions).

2. **Add `const corsHeaders = getCorsHeaders(req)` at the start of the serve() handler.**

3. **Add CORS headers to ALL responses.** The stripe-webhook currently returns plain responses without CORS headers. Update every `new Response(...)` call to include `{ headers: { ...corsHeaders, 'Content-Type': 'application/json' } }` (or just `{ headers: corsHeaders }` for plain text responses).

   Responses to update:
   - Line 44: `return new Response('Missing signature...', { status: 400 })` — add corsHeaders
   - Line 53: `return new Response('Invalid signature', { status: 400 })` — add corsHeaders
   - Line 151: `return new Response(JSON.stringify({ received: true }), ...)` — add corsHeaders
   - Line 157: `return new Response(\`Webhook Error: ...\`, { status: 400 })` — add corsHeaders

4. **Add OPTIONS preflight handling** at the start of the handler (before signature check):
   ```typescript
   if (req.method === 'OPTIONS') {
     return new Response('ok', { headers: corsHeaders })
   }
   ```

Note: Stripe webhook calls don't send an Origin header, so getCorsHeaders will default to the production domain — this is harmless and correct. The CORS headers won't affect Stripe's server-to-server calls, but they ensure that if someone tries to call this endpoint from a browser, CORS policy applies.
  </action>
  <verify>
- Search stripe-webhook/index.ts for `ALLOWED_ORIGINS` — should find the array
- Search for `getCorsHeaders` — should find definition and usage
- Count `new Response(` calls — every one should include corsHeaders in headers
- Verify OPTIONS handler exists
  </verify>
  <done>
The stripe-webhook edge function includes CORS headers on all responses. All 4 edge functions now have consistent dynamic origin validation — no wildcard CORS exists anywhere in the codebase.
  </done>
</task>

</tasks>

<verification>
1. Search all 4 edge function files for `'*'` in Access-Control-Allow-Origin — zero matches across all files
2. Search all 4 files for `ALLOWED_ORIGINS` — found in all 4
3. Search all 4 files for `getCorsHeaders` — found in all 4
4. For each file, count `new Response(` calls and verify each includes corsHeaders
</verification>

<success_criteria>
- Zero instances of `Access-Control-Allow-Origin: '*'` in any edge function
- All 4 edge functions define ALLOWED_ORIGINS with thepartypalace.in and localhost variants
- All 4 edge functions compute corsHeaders per-request via getCorsHeaders(req)
- Every Response object in every edge function includes corsHeaders
- stripe-webhook has OPTIONS preflight handler
</success_criteria>

<output>
After completion, create `.planning/phases/01-security/01-02-SUMMARY.md`
</output>
